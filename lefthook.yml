pre-commit:
  commands:
    check:
      glob: "src/**/*.{ts,tsx}"
      run: npx @biomejs/biome check --write

pre-push:
  commands:
    check:
      glob: "src/**/*.{ts,tsx}"
      run: npx @biomejs/biome check --write